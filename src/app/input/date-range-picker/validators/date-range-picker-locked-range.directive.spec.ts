import { DateRangePickerLockedRangeDirective } from './date-range-picker-locked-range.directive';

describe('DateRangePickerLockedRangeDirective', () => {
  it('should create an instance', () => {
    const directive = new DateRangePickerLockedRangeDirective();
    expect(directive).toBeTruthy();
  });
});
