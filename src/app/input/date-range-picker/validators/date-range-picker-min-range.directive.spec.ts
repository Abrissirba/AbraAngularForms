import { DateRangePickerMinRangeDirective } from './date-range-picker-min-range.directive';

describe('DateRangePickerMinRangeDirective', () => {
  it('should create an instance', () => {
    const directive = new DateRangePickerMinRangeDirective();
    expect(directive).toBeTruthy();
  });
});
